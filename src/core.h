#include <stdio.h>
#include <string.h>

#define EXIT_SUCCESS 0
#define EXIT_FAILURE 1

/**
 * is_keyword
 * @desc Check if the search string is in the keywords array
 * @author Laurence Roberts
 */
/*int is_keyword(char search[]) {
	int i;
	for (i=0; i<_keywords_size; i++)
		if (strcmp(search, _keywords[i]) == 0)
			return(1);
	return(0);
}*/